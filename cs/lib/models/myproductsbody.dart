// ignore_for_file: prefer_const_constructors, prefer_const_literals_to_create_immutables, use_key_in_widget_constructors

/*import 'dart:ffi';

import 'package:cs/models/product_list_container.dart';
import 'package:cs/models/products.dart';
import 'package:flutter/material.dart';

class MyProductsBody extends StatefulWidget {
  @override
  _MyProductsBodyState createState() => _MyProductsBodyState();
}

class _MyProductsBodyState extends State<MyProductsBody> {
  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context).size;
    return Padding(
      padding: const EdgeInsets.all(15.0),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          SizedBox(height: 20),
          Expanded(
              child: GridView.builder(
                  itemCount: books.length,
                  gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
                      crossAxisCount: 2,
                      childAspectRatio: 0.60,
                      crossAxisSpacing: 20,
                      mainAxisSpacing: 20),
                  itemBuilder: (context, index) => ProductsListContainer(
                    book: books[index],
                    press: () {},
                  )))
        ],
      ),
    );
  }
}*/
